HW-2 Submission by (Nitin Agrahara Ravikumar)

The program can be run by using the makefile provided.


make clean - will remove all *.o and binaries
make       - will compile and create the binary called smooth
./smooth   - will run the program

During execution 'y' will toggle between the original and manual rendering alrorithms 
and'f' will enable/disable flat shading. Gouroud is shown by default.